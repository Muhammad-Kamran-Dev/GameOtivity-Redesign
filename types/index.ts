export type CardDataProps = {
  imgUrl: string;
  title: string;
  description: string;
};

export type FeatureCardType = {
  img: string;
  title: string;
  text: string;
};

export type Founder = {
  name: string;
  imgUrl: string;
  designation: string;
  link?: string;
  left?: boolean;
};

export type imagesData = {
  imgUrl: string;
  altText: string;
};

export type GamesPerk = {
  heading: string;
  title: string;
  imgUrl: string;
  desc: string;
};
